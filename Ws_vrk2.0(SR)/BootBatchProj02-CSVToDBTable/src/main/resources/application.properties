# Start the batch processing only when launcher.lanuch(-) method is called
spring.batch.job.enabled=true
# Initialize the schema for JobRepository
spring.batch.jdbc.initialize-schema=always


#Datasource (Hikari cp by default)
spring.datasource.dbcp2.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/SPBMS
spring.datasource.username=root
spring.datasource.password=@Vamshi57
